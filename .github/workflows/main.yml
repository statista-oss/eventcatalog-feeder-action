on: [push]

jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - uses: statista-oss/eventcatalog-feeder-action@main
        with:
          domain: foobar
          domain-slug: foobar
          catalog-repo-url: https://github.com/PIT-Shared/api-catalog